import size from "./size"
import breakpoints from "./breakpoint"
import color from "./color"
const Theme = {
    body: '#000000',
    text: '#FAFAFA',
    toggleBorder: '#6B8096',
    background: '#0F1114',
    border: 'gray',
    size,
    breakpoints,
    color,
}

export default Theme
